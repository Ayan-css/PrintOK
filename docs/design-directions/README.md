# Design directions

`example.html` compares five visual directions for the PrintOk site. It is an
internal design mock-up, so it no longer ships with the public site: it used to
be served at `/example` on the production domain, where it showed invented
figures next to the real product.

Open it locally with its stylesheet beside it, e.g. `npx serve docs/design-directions`.
